## spec_packets/SPRINT_20260214_M0_M1.md

# Blood Contract PoC — Sprint Packet (Rolling)
Date: 2026-02-14
Owner: Planner (Game Design)
Source of Truth: Notion (DB_SYSTEMS / DB_CONTENT / DB_EXPERIMENTS / DB_BUILDS + GDD pages)
Scope filter: `PoC Scope = true`

## Immutable Pillars (Non-Negotiable)
1) Intent Combat
2) Synergy
3) ShortRun
4) Mobile

---

## PoC Definition of Done (DoD) — Target
See: `blood_contract/P0_POC_DEFINITION_OF_DONE.md`

---

# M0 Sprint Packet

## M0 Goal
Clean Godot 4 project skeleton + Android export baseline. No gameplay.

### MUST
- Folder structure:
  - `res://scenes/` `res://scripts/` `res://data/` `res://ui/` `res://audio/` `res://art/`
- Autoloads:
  - `GameApp` (app state)
  - `RunManager` (run lifecycle)
  - `DataRepo` (loads JSON/Resources)
- InputMap actions:
  - `move_stick`, `attack_btn`, `dash_btn`, `skill1_btn`, `pause`
- UI flow: `MainMenu -> Start -> loads TestArena`
- Android export preset saved
- Deliverables per milestone:
  - runnable project
  - `CHANGELOG.md`
  - `HOW_TO_TEST.md` referencing DB_EXPERIMENTS

### SHOULD
- Minimal debug overlay placeholder scene (even if values are dummy)
- DataRepo schema validation stub (warn if missing expected JSON keys)

### WON'T
- Gameplay systems (combat/AI/cards/rooms)

### Notion keys (reference)
- DB_BUILDS: `poc-0.1.0` exists (seed). For M0, create `poc-0.1.1` when build is exportable.
- DB_EXPERIMENTS: use TC01..TC10 as test list references (even if not runnable yet).

### Exit criteria
- Runs on desktop.
- Android preset exists; export works if templates/SDK installed.

---

# M1 Sprint Packet

## M1 Goal
Player controller + HP/Damage + hit detection + debug HUD. No enemy AI yet.

### MUST
- Player (`CharacterBody2D`)
  - move (stick)
  - attack (auto-aim assisted)
  - dash (invuln window param)
- Combat plumbing:
  - `HealthComponent`
  - `Hurtbox` / `Hitbox`
  - damage numbers optional (debug)
- Debug HUD:
  - FPS, run timer, current room
  - synergy levels placeholder

### SHOULD
- Basic camera follow + bounds
- Simple dummy target (training dummy) to validate Hitbox/Hurtbox

### WON'T
- Enemy intent state machine
- Spike wall
- Cards/synergy effects

### Tests to run (DB_EXPERIMENTS)
- (smoke) TC07 runtime measurement scaffolding: run timer visible
- (prep) TC09 performance scaffolding: FPS visible

### Exit criteria
- Player can move/attack/dash and see debug HUD; damage system demonstrably works.
